version: '3'
services:
  dev-db:
    image: postgres:latest
    container_name: dev-db
    ports:
      - '6500:5432'
    volumes:
      - progresDB:/data/postgres
    env_file:
      - ./.env
volumes:
  progresDB:
